<UserControl
  x:Name="root"
  x:Class="ContentTypeTextNet.MnMn.MnMn.View.Controls.ScaleSlider"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"

  xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"

  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  mc:Ignorable="d"
  d:DesignHeight="300" d:DesignWidth="300"
>
  <StackPanel Orientation="Horizontal">
    <Slider
      Minimum="{Binding Minimum, RelativeSource={RelativeSource AncestorType=UserControl}}"
      Maximum="{Binding Maximum, RelativeSource={RelativeSource AncestorType=UserControl}}"
      Value="{Binding Value, RelativeSource={RelativeSource AncestorType=UserControl}}"
      Width="100"
    />
    <ToggleButton x:Name="commnadScale">
      <TextBlock Text="{Binding Value, RelativeSource={RelativeSource AncestorType=UserControl}, StringFormat=x0.00}" />
    </ToggleButton>
    <Popup StaysOpen="False" AllowsTransparency="True"  PopupAnimation="Fade" IsOpen="{Binding IsChecked, ElementName=commnadScale, Mode=TwoWay}" PlacementTarget="{Binding ElementName=commnadScale}" Placement="Bottom" >
      <Border BorderBrush="Red" BorderThickness="2" Background="Green">
        <StackPanel>
          <TextBlock Text="{Binding Value, RelativeSource={RelativeSource AncestorType=UserControl}, StringFormat=x0.00}" />
          <ItemsControl ItemsSource="{Binding ItemsSource, RelativeSource={RelativeSource AncestorType=UserControl}}" />
        </StackPanel>
      </Border>
    </Popup>
  </StackPanel>
</UserControl>
