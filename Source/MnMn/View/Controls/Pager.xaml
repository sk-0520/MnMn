<UserControl
  x:Class="ContentTypeTextNet.MnMn.MnMn.View.Controls.Pager"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"

  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  mc:Ignorable="d"
  d:DesignWidth="300"
  d:DesignHeight="20"
>
  <UserControl.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/View/Resources/ImageDictionary.xaml" />
      </ResourceDictionary.MergedDictionaries>
      <Style x:Key="CommandButton" TargetType="Control">
        <Style.Triggers>
          <DataTrigger Binding="{Binding Items.Count, ElementName=PART_Items}" Value="0">
            <Setter Property="Visibility" Value="Collapsed"/>
          </DataTrigger>
        </Style.Triggers>
      </Style>

    </ResourceDictionary>
  </UserControl.Resources>
  <DockPanel>
    <RepeatButton
      DockPanel.Dock="Left"
      CommandTarget="{Binding ElementName=PART_ScrollViewer}"
      Command="ScrollBar.LineLeftCommand"
      Style="{StaticResource CommandButton}"
    >
      <Image Source="/Resources/Common/View/Left.png" Style="{StaticResource SmallIconImage}" />
    </RepeatButton>
    <Button DockPanel.Dock="Left" Click="PrevButton_Click" Style="{StaticResource CommandButton}">
      <Image Source="/Resources/Pager/Prev.png" Style="{StaticResource SmallIconImage}" />
    </Button>
    <Button DockPanel.Dock="Left" Click="NextButton_Click" Style="{StaticResource CommandButton}">
      <Image Source="/Resources/Pager/Next.png" Style="{StaticResource SmallIconImage}" />
    </Button>
    <RepeatButton
      DockPanel.Dock="Right"
      CommandTarget="{Binding ElementName=PART_ScrollViewer}"
      Command="ScrollBar.LineRightCommand"
      Style="{StaticResource CommandButton}"
    >
      <Image Source="/Resources/Common/View/Right.png" Style="{StaticResource SmallIconImage}" />
    </RepeatButton>
    <ScrollViewer
      x:Name="PART_ScrollViewer"
      HorizontalScrollBarVisibility="Hidden"
      VerticalScrollBarVisibility="Disabled"
    >
      <ItemsControl x:Name="PART_Items" ItemsSource="{Binding PageItems, RelativeSource={RelativeSource AncestorType=UserControl}}">
        <ItemsControl.ItemsPanel>
          <ItemsPanelTemplate>
            <VirtualizingStackPanel Orientation="Horizontal" />
          </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        <ItemsControl.ItemTemplate>
          <DataTemplate>
            <RadioButton
              Style="{StaticResource {x:Type ToggleButton}}"
              Content="{Binding PageNumber}"
              Command="{Binding Command, RelativeSource={RelativeSource AncestorType=UserControl}}"
              CommandParameter="{Binding}"
              IsChecked="{Binding IsChecked}"
            />
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>
    </ScrollViewer>
  </DockPanel>
</UserControl>
