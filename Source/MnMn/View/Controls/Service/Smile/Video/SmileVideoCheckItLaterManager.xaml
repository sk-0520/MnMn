<!--
  呼び出し元から DataContext を設定して使用。
-->
<UserControl
  x:Class="ContentTypeTextNet.MnMn.MnMn.View.Controls.Service.Smile.Video.SmileVideoCheckItLaterManager"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:app_view_controls_service_smile_video="clr-namespace:ContentTypeTextNet.MnMn.MnMn.View.Controls.Service.Smile.Video"

  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  mc:Ignorable="d"
  d:DesignHeight="300" d:DesignWidth="300"
>
  <Grid>
    <TabControl>
      <TabControl.Resources>
        <Style TargetType="app_view_controls_service_smile_video:SmileVideoFinderControl">
          <Setter Property="FooterContent">
            <Setter.Value>
              <Button Command="{Binding RemoveCheckedVideosCommand}">チェック項目を削除</Button>
            </Setter.Value>
          </Setter>
        </Style>
        <Style x:Key="InnerTab" TargetType="TabControl" BasedOn="{StaticResource {x:Type TabControl}}">
          <Setter Property="ItemTemplate">
            <Setter.Value>
              <DataTemplate>
                <app_view_controls_service_smile_video:SmileVideoFinderControl />
              </DataTemplate>
            </Setter.Value>
          </Setter>
        </Style>
      </TabControl.Resources>
      <TabItem Header="全て" DataContext="{Binding CheckItLaterFinder}" >
        <app_view_controls_service_smile_video:SmileVideoFinderControl />
      </TabItem>
      <TabItem Header="手動" DataContext="{Binding CheckItLaterFinder}" >
        <app_view_controls_service_smile_video:SmileVideoFinderControl />
      </TabItem>
      <TabItem Header="マイリスト差分" DataContext="{Binding MylistBookmarkFinderItems}">
        <TabControl Style="{StaticResource InnerTab}" />
      </TabItem>
      <TabItem Header="ユーザー投稿差分" DataContext="{Binding UserBookmarkFinderItems}">
        <TabControl Style="{StaticResource InnerTab}" />
      </TabItem>
      <TabItem Header="タグ差分" DataContext="{Binding WordBookmarkFinderItems}">
        <TabControl Style="{StaticResource InnerTab}" />
      </TabItem>

    </TabControl>
  </Grid>
</UserControl>
