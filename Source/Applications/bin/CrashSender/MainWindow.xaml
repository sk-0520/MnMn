<Window 
  x:Class="ContentTypeTextNet.MnMn.Applications.CrashSender.MainWindow"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  mc:Ignorable="d"
  Title="MainWindow" 
  Width="300"
  Height="450" 
>
  <Grid>
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition />
        <RowDefinition />
        <RowDefinition />
      </Grid.RowDefinitions>
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="Auto" />
        <ColumnDefinition Width="*" />
      </Grid.ColumnDefinitions>

      <Label Grid.Row="0" Grid.Column="0" Content="クラッシュレポート" />
      <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding ReportFilePath}" />

      <Label Grid.Row="1" Grid.Column="0" Content="レポート内容" />
      <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding ReportFileData}" />

      <Label Grid.Row="2" Grid.Column="0" Content="追加情報" />
      <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding ReportInformation}" />

      <Label Grid.Row="3" Grid.Column="0" Content="連絡先" />
      <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding ContactAddress}" />

    </Grid>
  </Grid>
</Window>
